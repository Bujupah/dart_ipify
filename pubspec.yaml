name: dart_ipify
description: This package uses the remote IPify web service in order to get the public IP address.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
